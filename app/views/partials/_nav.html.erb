<nav>
<%if user_signed_in? %>
  <%= link_to "Edit", edit_user_registration_path, class: "nav-btn edit-btn"%>
  <%= button_to "Sign Out",
      destroy_user_session_path,
      method: :delete,
      style: 'border: none;
              border-radius: 5px;
              font-size: 16px;
              background: transparent;
              color: #ffffff;' 
              %>
<% else %>
  <%= link_to "Sign up", new_user_registration_path, class: "nav-link"%>
  <%= link_to "Log in", new_user_session_path, class: "nav-link" %>
<% end %>
</nav>